/**
 * Audio context management utilities
 * Extracted from src/lib/audioContext.ts for better organization
 */

export { 
  handleFirstUserInteraction, 
  ensureAudioContextRunning,
  getAudioContext,
  resetAudioContext
} from '../audioContext';