# Claude Code configuration for terase project
# This file grants necessary permissions for Claude to work with the project

# Allow Bash tool for npm operations and build tasks
allowedTools:
  - bash

# Project-specific preferences
preferences:
  # Allow npm install and package management operations
  packageManagement: true
  
  # Allow build and test operations
  buildOperations: true